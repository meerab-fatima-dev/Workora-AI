# Copy this file to .env.local and fill in your own value.
# Not required for local development — the app defaults to http://127.0.0.1:8000
# if this is left unset. Only needed once the backend is deployed elsewhere.

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000